/**
 * Represents the _Image_ model, which holds a string imageUri
 *
 * ```yaml
 * id: number;
 * lodgingId: number;
 * imageUri: string;
 * ```
 */
export interface Image {
  id: number;
  lodgingId: number;
  imageUri: string;
}
