/**
 * Represents the _Image_ model
 *
 * ```yaml
 * id: number;
 * lodgingId: number;
 * imageUri: string;
 * ```
 */
export interface Image {
  id: number;
  lodgingId: number;
  imageUri: string;
}
